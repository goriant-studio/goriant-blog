---
description: commit all changes and push to deploy (git add + commit + push)
---

// turbo-all

1. Stage, commit and push all changes. If the user provided a commit message use it, otherwise use a generic one.
```
bash quick_deploy.sh "<commit message here>"
```
